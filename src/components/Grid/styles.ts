import styled from 'styled-components';

export const GridContainer = styled.div`
  margin: 0;
  max-width: var(--max-width);
  padding: 120px 0 0;
  position: relative;
  width: 100%;
  z-index: 3;
`;