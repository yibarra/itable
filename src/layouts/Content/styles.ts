import styled from 'styled-components';

export const ContentContainer = styled.div`
  float: left;
  height: auto;
  padding: 0 20px 60px;
  position: relative;
  text-align: center;
  width: 100vw;
`;