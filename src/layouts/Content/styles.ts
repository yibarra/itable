import styled from 'styled-components';

export const ContentContainer = styled.div`
  float: left;
  height: auto;
  padding: 60px 20px;
  position: relative;
  text-align: center;
  width: 100vw;
`;